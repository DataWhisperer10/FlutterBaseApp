import 'package:flutter/material.dart';

class Settings8 extends StatefulWidget {
  const Settings8({super.key});

  @override
  State<Settings8> createState() => _Settings8State();
}

class _Settings8State extends State<Settings8> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Settings 8"),
      ),
    );
  }
}
